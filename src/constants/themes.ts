import { PaletteType } from '@material-ui/core';

const light = {
  palette: {
    type: 'light' as PaletteType
  }
};

const dark = {
  palette: {
    type: 'dark' as PaletteType
  }
};

export {light, dark};