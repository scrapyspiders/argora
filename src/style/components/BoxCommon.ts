import styled from 'styled-components';

const Main = styled('div')`
  display: flex;
`;

const LeftSide = styled('div')`
  padding: 10px 20px 0px 20px;
`;

const RightSide = styled('div')`
  flex: 1;
`;

export {Main, LeftSide, RightSide};