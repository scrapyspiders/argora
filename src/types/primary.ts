type T_txid = string;
type T_walletAddr = string;
type T_topic = string;
type T_loading = "form" | "timeline";
type T_timeline = "main" | "comments" | "profile";

export type {T_txid, T_walletAddr, T_topic, T_loading, T_timeline};